---
- name: Prepare nodes
  hosts: all
  become: true
  tasks:
    - name: Install pip + base packages
      ansible.builtin.apt:
        name:
          - python3-pip
          - gnupg
          - curl
          - ca-certificates
        state: present
        update_cache: true
