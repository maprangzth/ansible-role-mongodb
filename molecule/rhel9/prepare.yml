---
- name: Prepare nodes
  hosts: all
  become: true
  tasks:
    - name: Install pip + base packages
      ansible.builtin.dnf:
        name:
          - python3-pip
          - gnupg2
          - curl
          - ca-certificates
        state: present
